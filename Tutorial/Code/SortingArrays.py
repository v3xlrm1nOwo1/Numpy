import numpy as np

arr = np.array([4, 1, 5, 2, 3])

print(np.sort(arr))
print(arr)

arr = np.array([True, False, True, False])
print(np.sort(arr))

arr = np.array(['Python', 'C', 'Java', 'SQL'])
print(np.sort(arr))